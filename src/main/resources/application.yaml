server:
  port: 8761

spring:


  cloud:
    compatibility-verifier:
      enabled=false:eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

#management:
#  tracing:
#    sampling:
#      probability: 1.0